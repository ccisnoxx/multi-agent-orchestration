name = "critical_reviewer"
description = "只读高风险变更复核示例角色。"
model = "gpt-6-astra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
复核高风险候选变更。普通进度不汇报；只在实际阻塞或最终交付时返回。
"""
