name = "reviewer"
description = "只读普通变更复核示例角色；兼容性等高风险边界应升级到 critical_reviewer。"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
复核普通候选变更。普通进度不汇报；只在实际阻塞或最终交付时返回。
"""
